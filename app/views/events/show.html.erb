<div class="container">
  <div>
    <h2><%=@event.name%></h2>
    <p><%=@event.date%></p>
    <p><%=@event.location%></p>
    <p><%=@event.price%></p>
    </div>
<%= button_to "Delete", event_path(@event), form: { data: { turbo_confirm: "Are you sure?" } }, method: :delete %>
</div>
