<div class="container">
  <h1>All events</h1>
  <% @events.each do |event| %>
    <%= link_to event_path(event), class:"text-decoration-none" do %>
    <div>
      <h2><%=event.name%></h2>
      <p><%= event.date%></p>
    </div>
    <% end %>
  <% end %>
</div>
